/**
 * \file
 * \brief Networking process
 */

#include <stdio.h>
#include <aos/aos.h>

int main(int argc, char *argv[])
{
    debug_printf("Networking process started!\n");
    setbuf(stdout, NULL);
    /*char new_char;
    while (1) {
        scanf("%c", &new_char);
        if (new_char != 0) {
            printf("%c", new_char);
            if (new_char == '\r') {
                printf("\n");
            }
        }
    }*/
    return 0;
}
